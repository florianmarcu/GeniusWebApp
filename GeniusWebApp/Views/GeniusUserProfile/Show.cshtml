@model List<GeniusWebApp.Models.GeniusUserProfile>
@{
    ViewBag.Title = "Show";
}

@if (Model.Count == 0)
{
    <p>No profile found</p>
}

@foreach(var profile in Model)
{
    <p>profile.LastName</p>
    <p>profile.FirstName</p>
}
