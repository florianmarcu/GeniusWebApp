@model GeniusWebApp.Models.Group
@{
    ViewBag.Title = "New";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Creează un grup</h2>

@using (Html.BeginForm("NewGroup", "Home"))
{
    <div class="form-group">
        @Html.LabelFor(group => group.Name)
        @Html.TextBoxFor(group => group.Name, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(group => group.Description)
        @Html.TextBoxFor(group => group.Description, new { @class = "form-control" })
    </div>
    <button type="submit" class="btn btn-primary">GATA</button>
    @*<form method="post" action="/Home/NewGroup">
            <label>Nume</label>
            <br />
            <input type="text" name="Name" />
            <br /><br />
            <label>Descriere</label>
            <br />
            <input type="text" name="Description" />
            <br /><br />
            <button type="submit">Adauga grup</button>
        </form>*@
}

